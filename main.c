#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "aes.h"

int main(int argc, char* argv[]) {
    // Parse command-line arguments
    // Read input file and convert to blocks
    // Generate random 128-bit encryption key
    // Call aes_key_expansion() to generate round keys
    // Call aes_encrypt() or aes_decrypt() as needed
    // Write the output to the output file
    // Measure and report the encryption/decryption time
    return 0;
}